# Article generator dependencies (lightweight)
# Works with Python 3.10-3.12
# Install: pip install -r requirements-article-generator.txt

-r requirements-core.txt

# Article generator only needs pyyaml, which is in requirements-core.txt
# No additional dependencies required
